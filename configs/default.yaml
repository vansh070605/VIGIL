data:
  dataset_path: "dataset"
  image_size: [256, 256]
  batch_size: 16
  num_workers: 4

model:
  name: "autoencoder"
  latent_dim: 128
  in_channels: 1

training:
  epochs: 50
  learning_rate: 0.001
  weight_decay: 0.0001
  device: "cuda" # or "cpu"
  save_dir: "experiments/autoencoder"
